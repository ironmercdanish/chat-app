<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Chat</title>
    <link rel="stylesheet" href="/style.css"> <!-- Link to CSS -->
</head>
<body>
    <main>
        <h2>Edit Your Chat: <%= chat._id %></h2>
        <p>This chat was sent from <strong><%= chat.from %></strong> to <strong><%= chat.to %></strong></p>
        <form method="post" action="/chats/<%= chat._id %>?_method=PUT">
            <textarea name="msg" placeholder="Update your message here..."><%= chat.msg %></textarea>
            <button type="submit">Edit</button>
        </form>
        <a href="/chats">Back to Chats</a>
    </main>
</body>
</html>
